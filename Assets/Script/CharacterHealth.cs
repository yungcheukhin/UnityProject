using UnityEngine;
using System.Collections;

public class CharacterHealth : MonoBehaviour {
    int current_health = 100;
	void Update () {
	    if(current_health < 0)
        {
            
        }
	}
}
